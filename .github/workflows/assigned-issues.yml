name: Move assigned issues into In-Progess

on:
  issues:
    types: [assigned]

jobs:
  automate-project-columns:
    runs-on: ubuntu-latest
    steps:
      - uses: seanhagstrom/github-project-automation-plus
        with:
          project: Test
          column: In Progress
          repo-token: ${{ secrets.TEST_PROJECT_TOKEN }}
